/*!
    二叉搜索树(Binary Search Tree, BST)
    左小右大
    - `左子树` 的 `所有节点的值` 都 `小于` 该节点的值
    - `右子树` 的 `所有节点的值` 都 `大于` 该节点的值
*/

pub struct BinarySearchTree<T> {
    data: T,
}

impl<T> BinarySearchTree<T> {}
